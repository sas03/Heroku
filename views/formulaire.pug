quand on appelle formulaire, il par t sur la page layout  et injecter tout ca, et sur le block content, ca ajoute tout automatiquement
extends layout
block content
    form(method="POST")
        input(type="text" name="name")
        input(type="text" name="first_name")
        button(type="submit") Valider